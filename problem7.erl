%% Copyright
-module(problem7).
-author("Novakov").

-mode(compile).
%% API
-export([]).

erasto(N) ->
        N.

solve(N)->
        N.

main(_)->
        io:format("~p\n", [solve(6)]).
